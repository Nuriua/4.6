#include <iostream>

using namespace std;

int main()
{   
    float a1, a2, b1, b2, c1, c2, x, y;
    cout<<"Введите a1, a2, b1, b2, c1, c2";
    cin>>a1;
    cin>>a2;
    cin>>b1;
    cin>>b2;
    cin>>c1;
    cin>>c2;
    x=((c1+c2)-(b1+b2))/(a1+a2);
    cout<<x;
    return 0;
}
